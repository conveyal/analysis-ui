import React from 'react'

// Stores a function that will be called and the results rendered onto the map
export const Children = React.createContext(() => null)

// Stores a map click handler
export const ClickHandler = React.createContext(() => {})
