### Fixes

- Prevent Regional Analysis comparison map from flashing when the statuses are updated.
- Fix bug that disabled deleting a running Regional Analysis.
- Allow navigating to a Project that has had its Bundle deleted.

### Changes

- Disable deleting a Bundle while a Project is using it.
- Reformat display of shapefile upload errors.
