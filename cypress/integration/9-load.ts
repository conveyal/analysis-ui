describe.skip('Load Tests', () => {
  before(() => {
    // populate database with 100+ of each entity type (duplicate bundle/opportunties so the data doesn't double)
  })

  it('can read/edit a region')

  it('can read/edit a bundle')

  it('can read/edit an opportunity dataset')

  it('can read/edit a project')

  it('can read/edit a scenario')

  it('can read/edit a modification')

  it('can read/edit a timetable')

  it('can read/edit a preset')

  it('can read/edit a regional analysis')
})
